## What's new

- **The disk image says to quit MemoryClip first.** Taking an update opens the
  new version's disk image while the old one is still running, and macOS will
  not replace an app mid-flight — so the drag onto Applications failed with
  nothing on screen to explain it. The window now carries the instruction, and
  where to find Quit, laid out along the drag itself.
